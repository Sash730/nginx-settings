'use strict';
var path = require('path');

module.exports = {
  domain:'http://localhost:9090',

  // Root path of server
  //root: path.normalize(__dirname + '/../../..'),

  // Server port
  port: 9090,

  // Server IP
  ip: '0.0.0.0'
};