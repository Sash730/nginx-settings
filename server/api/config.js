'use strict';
var path = require('path');

module.exports = {
  domain:'http://localhost:8080',

  // Root path of server
  //root: path.normalize(__dirname + '/../../..'),

  // Server port
  port: 8080,

  // Server IP
  ip: '0.0.0.0'
};