{
  "name": "nginxsettings",
  "version": "0.0.1",
  "readme": "README.md",
  "dependencies": {
    "body-parser": "^1.13.3",
    "express": "^4.15.2",
    "express-jwt": "^3.0.0",
    "express-session": "^1.11.3",
    "method-override": "^2.3.5",
    "morgan": "~1.7.0",
    "request": "^2.81.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=5.2"
  },
  "scripts": {
    "docker": "docker-compose -f docker-compose.yml build && docker-compose -f docker-compose.yml up -d"
  }
}
